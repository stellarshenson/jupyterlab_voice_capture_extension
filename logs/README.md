# Logs

Build and test output captured during local development.

- `make-install.log` - full `make install` run (nodeenv bootstrap, prod build, wheel install)
- `jest.log` - `make test` (Jest) frontend unit tests
- `pytest.log` - server-side pytest run (websocket / FIFO sink tests)
